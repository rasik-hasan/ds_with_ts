export const testminSubArrayLen = () => {
  console.log("min sub array len");

  const target = 7,
    nums = [2, 3, 1, 2, 4, 3];

  function minSubArrayLen(target: number, nums: number[]): number {
    let minLength = Infinity;
    let left = 0;
    let right = 0;

    while (left <= right) {}
  }

  console.log(minSubArrayLen(target, nums));
};
